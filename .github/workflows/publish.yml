name: publish

# Netlify is the live host for claimfreepatch.netlify.app.
# GitHub Pages was failing at configure-pages. Disabled until Pages is
# enabled in repo Settings → Pages. Deploy via Netlify repo link or drag-drop.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages disabled. Deploy via Netlify link or drag-drop index.html."
